@model OPMS.ViewModels.ContactMultiVM
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_ContactLayout.cshtml";
}
@if (Model != null)
{
    foreach (var item in Model.ContactPageViewModel)
    {
        <div class="jumbotron">
            <h1>Hello, world!</h1>
            <p>...</p>
            <p><a class="btn btn-primary btn-lg" href="#" role="button">Learn more</a></p>
        </div>
    }
}
@RenderPage("Contact")



