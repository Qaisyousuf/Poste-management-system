@model OPMS.ViewModels.RoleViewModel
@{
    ViewBag.Title = "Roles";
}
<hr />
<h4>Détails du rôle <button disabled class="btn btn-primary">@Model.Name </button></h4>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Éditer", "Edit", new { id = Model.Id }, new { @class = "btn btn-default" }) |
    <a class="btn btn-primary" href="/OPMSAdmin/Roles/Index">Retour à la liste principale</a>
</p>
