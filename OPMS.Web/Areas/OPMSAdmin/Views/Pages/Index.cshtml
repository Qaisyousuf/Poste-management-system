@model IEnumerable<OPMS.ViewModels.PageViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<br />
<h4> Welcome to the Page Creation Page </h4>
<br />
<p>
    @*//<button class="btn btn-primary"><a href="/Pages/Create"></a>Create</button>*@
    <a class="btn btn-primary" href="/OPMSAdmin/Pages/Create">Create New Page</a>
</p>

<hr />
<table id="dataTable" class="table table-bordered table-hover table-responsive table-striped">
    <thead class="bg-primary">
        <tr>
            <th><i class="glyphicon glyphicon-align-center"></i> Ttitle</th>
            <th><i class="glyphicon glyphicon-link"></i> Slug</th>
            <th><i class="glyphicon glyphicon-menu-right"></i> Meta Description</th>
            <th><i class="glyphicon glyphicon-menu-right"></i> Meta Keywords</th>
            <th><i class="glyphicon glyphicon-menu-right"></i> Sidebar Name</th>
            <th><i class="glyphicon glyphicon-wrench"></i> Actions</th>
        </tr>
    </thead>
</table>
@section scripts{
    <script>
        $(document).ready(function () {
            $("#dataTable").DataTable({
                "info": false,
                "lengthMenu": [[5, 10, 25, 50, -1], [5, 10, 25, 50, "All"]],
                "dom": "lfrtiBp",
                "buttons": [
                    "print"
                ],
                "ajax": {
                    "url": "/OPMSAdmin/Pages/GetPagesData",
                    "type": "Get",
                    "dataType":"json"
                },
                "columns": [
                    { "data": "Title" },
                    { "data": "Slug" },
                    { "data": "MetaDescription" },
                    { "data": "Sidebars.Name" },
                    { "data":"MetaKeywords"},
                    {
                        "data": "actions",
                        "render": function (data, type, row, meta) {
                            data = '<a class="btn btn-primary btn-bg" href="/OPMSAdmin/Pages/Edit/' + row.Id + '"><i class="glyphicon glyphicon-edit"></i> Edit Page</a> | <a class="btn btn-danger btn-bg" href="/OPMSAdmin/Pages/Delete/' + row.Id + '"><i class="glyphicon glyphicon-trash"></i> Delete Page</a> <a class="btn btn-info btn-bg" href="/OPMSAdmin/Pages/Details/' + row.Id + '"><i class="glyphicon glyphicon-info-sign"></i> Page Info</a>';
                            //  data = '<a class="btn btn-info btn-sm" href="/Users/Edit/' + row.Id + '">Edit</a> | <button class="btn btn-danger btn-sm" onclick="return Delete(' + row.Id + ')">DeletePage</button>';
                            return data;
                        }
                    },


                ]
            })
        });
    </script>
}

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Slug)
            </th>


            <th>
                @Html.DisplayNameFor(model => model.SidebarId)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Slug)
            </td>


            <td>
                @Html.DisplayFor(modelItem => item.Sidebars.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }

    </table>*@
